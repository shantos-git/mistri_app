import 'package:flutter/material.dart';

class PlumbingServiceScreen extends StatelessWidget {
  const PlumbingServiceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
